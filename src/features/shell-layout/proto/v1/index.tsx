import { ShellLayoutFrame } from './ShellLayoutFrame';

/** Демо shell-layout: полный шаблон с боковой навигацией и демо-контентом (см. ShellLayoutFrame). */
export default function ShellLayoutProtoV1() {
  return <ShellLayoutFrame />;
}
